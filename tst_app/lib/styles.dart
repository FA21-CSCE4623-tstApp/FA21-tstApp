import 'package:flutter/material.dart';

Color appBackground = Color(0xfff9ecdd);
Color accentColor = mediumPurple;

Color lightBrown = Color(0xffbdad95);
Color mediumBrown = Color(0xffab906e);
Color darkBrown = Color(0xff443727);

Color lightPurple = Colors.deepPurple[200];
Color mediumPurple = Color(0xff7f6493);
Color darkPurple = Colors.deepPurple[800];

Color defaultTextColor = Colors.black54;
Color lightTextColor = Colors.white70;
Color defaultIconColor = Colors.black54;

EdgeInsets defaultScreenPadding = EdgeInsets.all(20.0);
EdgeInsets defaultSectionPadding = EdgeInsets.symmetric(vertical: 20.0);
EdgeInsets defaultTextPadding = EdgeInsets.only(top: 10.0, bottom: 10.0);

TextStyle defaultWidgetTextStyle = TextStyle(
  fontSize: 23.0,
  color: defaultTextColor,
);

BoxDecoration defaultRoundedCorners = BoxDecoration(
  color: lightPurple,
  borderRadius:
  BorderRadius.all(Radius.circular(10.0)),
);